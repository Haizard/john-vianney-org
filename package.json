{
  "name": "agape-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend/school-frontend-app && npm install && npm run build"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "html2canvas": "^1.4.1",
    "jspdf": "^3.0.1"
  }
}
